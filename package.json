{
  "name": "@luwang/native-mini-program",
  "version": "0.1.0-internal",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify": "node scripts/verify-package.mjs && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
